# Vercel AI Gateway — create a key in the Vercel dashboard (AI Gateway → API keys).
# The `gateway()` helper reads this by default. See:
# https://sdk.vercel.ai/providers/ai-sdk-providers/ai-gateway
AI_GATEWAY_API_KEY=your_api_key_here
